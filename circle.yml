machine:
  java:
    version: oraclejdk8
  services:
    - docker

dependencies:
  cache_directories:
    - "~/docker"
  override:
    - mvn -T 1C clean install -DskipTests=true
test:
  override:
    - mysql -u ubuntu -e "create database sample_spring_jpa;"
    - mvn test
